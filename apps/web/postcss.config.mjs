/**
 * Tailwind CSS v4.
 *
 * v4 không còn `tailwind.config.js`: mọi cấu hình (màu, font, breakpoint) khai
 * ngay trong CSS bằng `@theme`. Xem `app/globals.css`.
 */
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
